# 달모음 학급용 운영 안내

> 이 문서는 기존 Python/SQLite 서버 운영 방법입니다. **Supabase에서 운영하려면 [Supabase 운영 안내](SUPABASE_운영안내.md)를 사용하세요.**

28명이 각자 휴대폰에서 기록하고, 담임선생님이 학교 컴퓨터에서 모아 출력하는 버전입니다. 현재는 구현과 로컬 검증이 끝난 상태이며, 실제 인터넷 주소 발급·호스팅 가입·결제는 아직 하지 않았습니다.

## 매일 사용할 때

1. 선생님은 관리실에서 1~28번 학생의 이름을 입력합니다. 실명 대신 번호·별칭으로 운영해도 됩니다.
2. 학생별 접속 링크를 해당 가정에 전달하거나, **접속 안내 카드 출력**으로 QR 카드를 인쇄해 한 장씩 나눠 줍니다.
3. 학생과 부모님은 자기 링크·QR로 접속해 날짜, 시각, 달 사진 또는 달 그림을 저장합니다. 별도 회원가입은 없습니다.
4. 선생님은 관리실에서 관찰 월을 선택하고 새로고침하면 학생별 기록 일수·사진 수·마지막 관찰일·알게 된 점 작성 여부를 볼 수 있습니다.
5. 학생을 선택하고 **선택한 N명 출력**을 누르면 보고서가 새 창으로 열립니다. **인쇄 / PDF 저장**을 누르고 A4 세로, 머리글·바닥글 끄기를 선택합니다. 기본 보고서는 학생별 1장입니다. 긴 메모나 소감은 잘리지 않도록 추가 장으로 이어집니다.

접속 링크는 해당 학생 기록의 열쇠입니다. 링크를 가진 사람은 그 학생의 기록을 읽고 수정할 수 있으므로 전체 링크·QR 목록을 단체방에 올리지 마세요. 이름과 번호만으로 다른 학생의 기록에 들어가는 기능은 없습니다. 링크를 잘못 공유했다면 **기존 링크 폐기·재발급**을 사용합니다. 이전 링크와 기존 로그인은 해제되고 기록은 유지됩니다.

## 인터넷에서 운영하는 방법: Render 한 곳에 배포

학교 PC를 계속 켜 두지 않아도 되는 구성입니다. 웹앱과 SQLite 기록 저장소를 한 서비스에서 운영합니다. 준비된 `render.yaml`은 유료 웹 서비스와 1GB 영구 디스크를 함께 설정합니다. 별도 유료 도메인은 필요하지 않으며 서비스에서 제공하는 HTTPS 주소를 사용할 수 있습니다.

비용 참고: Render 공식 안내의 0.5 CPU / 512MB 웹 서비스 월 US$7와 영구 디스크 1GB 월 US$0.25를 합하면 기본 예상 비용은 **월 US$7.25**입니다. 이는 2026-09-07 확인한 안내에 따른 예상이며, 세금·환율·초과 사용량·요금 변경에 따라 실제 결제가 달라집니다. 서비스 생성 화면의 견적을 확인하고 결제 여부를 결정하세요. 무료 웹 서비스에는 이 앱에 필요한 영구 디스크를 붙일 수 없습니다.

공식 근거: [웹 서비스 비용](https://render.com/articles/render-vs-railway), [디스크 비용 안내](https://render.com/articles/how-much-does-cloud-application-hosting-cost-for-small-businesses), [영구 디스크](https://render.com/docs/disks), [무료 서비스 제한](https://render.com/docs/free), [배포 설정 파일](https://render.com/docs/blueprint-spec).

처음 한 번의 배포 절차:

1. GitHub 계정에 **비공개 저장소**를 만들고, 배포 ZIP을 푼 소스 파일을 저장소 최상위에 올립니다. ZIP 파일 자체를 올리는 것이 아니라 그 안의 파일을 올립니다. `data/`, `.sqlite3`, `secret.key`, 학생 백업 파일은 올리지 않습니다.
2. Render에 가입하고 GitHub 저장소 연결을 허용합니다. **New → Blueprint**에서 이 저장소를 선택하면 `render.yaml` 설정을 읽습니다.
3. `DAL_CLASS_NAME`에 학급 이름(예: 3학년 2반), `DAL_INITIAL_PASSWORD`에 선생님 비밀번호를 설정합니다. 비밀번호는 12자 이상이며 대화창이나 소스 파일에 적지 말고 Render의 비밀 환경변수 입력란에 넣습니다. 기본 학생 수는 28명입니다.
4. 웹 서비스와 **1GB 영구 디스크**가 모두 포함된 견적을 확인하고 배포합니다. 최초 실행 시 28명의 빈 학생 공간을 만들며, 재배포는 기존 학급과 기록을 유지합니다.
5. 발급된 HTTPS 주소의 `/teacher`에 접속합니다. 예를 들어 주소가 `https://서비스주소`라면 `https://서비스주소/teacher`가 선생님 관리실입니다.
6. Render 환경변수에 `DAL_PUBLIC_ORIGIN`을 실제 HTTPS 주소로 추가합니다. 마지막 `/` 없이 입력합니다. 이 값은 허용할 접속 주소를 제한하는 데 사용합니다. `DAL_SECURE_COOKIES=1`은 그대로 유지합니다.
7. 학생 이름을 입력한 뒤 **관리실의 비밀번호 변경**으로 실제 운영 비밀번호를 한 번 바꿉니다. 초기 환경변수 `DAL_INITIAL_PASSWORD`는 최초 생성에만 사용되며 이후 변경해도 현재 비밀번호는 바뀌지 않습니다. 첫 설정 후 Render에서 초기 비밀번호 환경변수를 제거할 수 있습니다.
8. 가상의 학생 한 명으로 휴대폰 두 대에서 기록·재접속·선생님 출력까지 확인한 뒤 학생별 링크를 배부합니다.

학생 수는 첫 배포 전에 `DAL_STUDENT_COUNT` 환경변수로 1~40명 범위에서 지정할 수 있습니다. 이미 생성된 학급에 대해서는 이 변수 변경으로 학생을 추가하거나 삭제하지 않습니다. 현재 프로그램은 담임 한 명이 운영하는 한 학급용입니다.

## 이 컴퓨터에서 먼저 확인하기

PowerShell에서 이 폴더로 이동한 뒤 아래 명령을 차례로 실행합니다. Python 3.12 이상을 사용하세요.

```powershell
python -m pip install -r requirements.txt
python server.py init --class-name "우리 반" --students 28
python server.py
```

`init` 과정에서 선생님 비밀번호를 두 번 직접 입력합니다. 이후에는 `python server.py`만 실행하면 됩니다. PC에서 `http://localhost:8000/teacher`를 열면 관리실을 볼 수 있습니다. 초기 비밀번호나 공용 학생 접속 코드는 소스에 들어 있지 않습니다.

같은 Wi-Fi의 휴대폰에서 잠깐 시험할 때는 서버를 종료한 뒤 `python server.py serve --host 0.0.0.0`으로 실행하고 `http://PC의IP주소:8000/teacher`를 PC에서 엽니다. 그 주소의 관리실에서 발급한 링크를 휴대폰으로 전달해야 합니다. `localhost`로 만든 링크는 다른 기기에서 사용할 수 없습니다. Windows 방화벽에 따라 접속 허용이 필요할 수 있습니다. 이 방식은 같은 네트워크에서 시험하기 위한 것이며 외부 인터넷에는 HTTPS 호스팅을 사용합니다.

## 저장, 백업, 복구

기록과 압축 사진은 `DAL_DATA_DIR`의 `classroom.sqlite3`에 저장됩니다. 기본값은 프로젝트의 `data/`, Render 설정값은 `/var/data/dalmoeum`입니다. 사진은 긴 변 640px 이하의 JPEG로 저장하며, 학생별 기록 데이터 상한은 20MB입니다. 서버 재시작·재배포 후에도 영구 디스크를 유지하면 기록이 남습니다. 디스크나 서비스를 삭제하면 별도 백업이 필요합니다.

- 선생님은 **학급 전체 백업**을 정기적으로 내려받습니다. 사진과 모든 달의 기록, 학생 명단, 선생님 비밀번호 해시가 포함된 SQLite 파일입니다. 현재 로그인 세션은 제외합니다. 개인 JSON 백업과는 다른 파일입니다.
- 백업은 읽을 수 있는 데이터 파일입니다. 선생님만 접근 가능한 폴더에 보관합니다. 자동 외부 백업은 아직 구성하지 않았으므로 이 버튼으로 보관해야 합니다.
- 학생은 자기 기록만 JSON으로 내보내거나 다시 불러올 수 있습니다. 이전 개인용 기록도 이렇게 옮길 수 있습니다. 불러오기는 해당 학생의 현재 기록 전체를 교체합니다.
- 다른 휴대폰에서 같은 전용 링크로 접속하면 서버에 저장된 기록을 불러옵니다. 화면은 접속 시 불러오며 다른 기기의 변경이 실시간으로 나타나지는 않습니다. 두 기기가 같은 버전을 수정하면 나중 저장은 차단하고 새로고침을 안내합니다.
- 인터넷이 끊기면 저장 실패를 표시하고 입력 내용을 현재 화면에 남깁니다. 연결 후 **저장 다시 시도**를 누르세요. 서버가 성공을 확인하기 전에는 저장 완료로 표시하지 않습니다. 충돌 시에는 기록 내보내기로 입력 내용을 따로 보관하고 새로고침하세요.

전체 백업을 복원해야 할 때에는 서버를 먼저 종료하고 운영 환경에서 실행합니다.

```powershell
python server.py restore --backup-file "백업파일의전체경로.sqlite3"
```

`복원`을 입력해야 교체되며, 교체 전 데이터도 `before-restore-*.sqlite3` 파일로 보관합니다. 현재 로그인은 모두 해제됩니다. 같은 서버의 `secret.key`가 유지되면 기존 학생 링크를 다시 사용할 수 있습니다. 새 서버로 복원해 비밀 키가 바뀌었다면 선생님 관리실에서 새 링크를 다시 나눠 줍니다. 복원된 데이터의 선생님 비밀번호를 사용합니다.

비밀번호를 잊었을 때는 서버 관리자 환경에서 `python server.py reset-password`를 실행해 바꿀 수 있습니다. 학생 이름 변경이나 링크 재발급은 평상시 선생님 관리실에서 처리합니다.

## 검증한 범위

`python -m unittest test_classroom -v`: 28명 동시 기록, 학생 간 접근 차단, 선생님 인증, 위조 요청 차단, 같은 기록 수정 충돌, 재시작 후 기록 유지, 백업 무결성, 링크 폐기, 비밀번호 변경, 모바일 사진 저장·실패 후 재시도, 선생님 28명 일괄 출력(5·6주 달력), QR 카드 생성.

`python verify_app.py`: 기존 개인용 저장·사진·수정·삭제·JSON 복원·모바일·인쇄 확인.

실제 외부 호스팅, 학교 네트워크, 휴대폰 카메라 권한은 배포 후 실제 기기에서 마지막으로 확인해야 합니다. 브라우저 자동화에서는 사진 파일 첨부와 휴대폰 크기의 화면으로 검증했습니다.
