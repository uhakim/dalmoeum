# Supabase 하나로 4반 운영하기

현재 학생 로그인은 **반·번호·학생별 4자리 PIN** 방식입니다. 아래 4반 전환 후 `supabase/migrations/202609070003_student_pin.sql`도 한 번 적용합니다. PIN은 선생님 관리실의 ‘접속 정보’에서 확인·재발급합니다. PIN을 5회 틀리면 선생님의 재발급이 필요합니다. 기존 링크는 유지됩니다. 학급 복원 시 학생 PIN은 새로 생성됩니다.

관리 계정은 `teacher1`, `teacher2`, `teacher3`, `teacher4`입니다. 각 계정이 하나의 반만 관리합니다. 학급 이름은 실제 학년·반 이름으로 지정할 수 있으며 로그인 계정 이름은 그대로 유지합니다. 다른 반의 학생 명단·기록·보고서·QR·백업·비밀번호를 읽거나 바꿀 수 없습니다. 학생은 자기 전용 링크로 접속합니다.

## 1. 이미 첫 번째 SQL을 설치한 경우

Supabase SQL Editor에서 새 쿼리를 열어 아래 파일 **전체**를 붙여넣고 한 번 실행합니다.

`supabase/migrations/202609070002_four_classrooms.sql`

이전에 실행한 `202609070001_classroom.sql`은 다시 실행하지 않습니다. 새 파일은 기존 학급을 첫 번째 반(`teacher1`)으로 전환하고 학생 기록·번호·전용 링크·비밀번호를 유지합니다. 아직 학급 초기화를 하지 않았어도 적용할 수 있습니다. 설치는 한 트랜잭션으로 처리되어 오류 시 변경 전체가 취소됩니다.

SQL Editor로 설치한 이 프로젝트에서는 `db push`로 같은 마이그레이션을 다시 적용하지 마세요. Edge Function 배포를 위한 `supabase link`는 사용할 수 있습니다. 완전히 새 프로젝트에 CLI로 설치할 때는 `db push`가 첫 번째와 두 번째 파일을 순서대로 적용하게 합니다.

## 2. 계정 4개와 학생 공간 생성

새 SQL 쿼리에 아래 내용을 붙여넣고 **비밀번호 4개를 각각 다른 실제 비밀번호로 바꾼 뒤** 실행합니다. 학급 이름과 학생 수도 수정할 수 있습니다.

```sql
select * from public.dal_initialize_four(
  array[
    'REPLACE_WITH_PASSWORD_1',
    'REPLACE_WITH_PASSWORD_2',
    'REPLACE_WITH_PASSWORD_3',
    'REPLACE_WITH_PASSWORD_4'
  ],
  array['3학년 1반', '3학년 2반', '3학년 3반', '3학년 4반'],
  array[28, 28, 28, 28]
);
```

비밀번호는 12자 이상, UTF-8 72바이트 이하입니다. SQL 문자열의 작은따옴표는 두 번(`''`) 입력합니다. 실제 비밀번호가 든 SQL을 소스 파일이나 공유 문서에 저장하지 마세요. 위 예시 문구는 운영 비밀번호로 쓰지 않습니다.

실행 결과에 계정 4개가 표시됩니다.

| classroom_id | login_id | 담당 |
|---|---|---|
| 1 | teacher1 | 첫 번째 학급 |
| 2 | teacher2 | 두 번째 학급 |
| 3 | teacher3 | 세 번째 학급 |
| 4 | teacher4 | 네 번째 학급 |

`created=true`는 새로 생성했다는 뜻입니다. `created=false`는 이미 존재해 그대로 유지했다는 뜻입니다. **이미 만들어둔 첫 번째 반은 기존 비밀번호·학급 이름·학생 수가 유지되며 위 배열의 첫 번째 값으로 덮어쓰지 않습니다.** 반 이름과 비밀번호는 해당 선생님이 로그인 후 바꿀 수 있습니다. 학생 수는 처음 생성할 때 정합니다(반별 1~40명).

이 설정 함수는 데이터베이스 관리자만 SQL Editor에서 실행할 수 있습니다. 웹 화면의 선생님 계정으로 다른 계정을 추가하거나 다섯 번째 반을 만들 수 없습니다.

## 3. 화면과 Edge Function 배포

처음 배포하는 경우 [Supabase 운영 안내](SUPABASE_운영안내.md)의 화면 배포 파일 생성부터 이어갑니다. 이미 배포했다면 새 화면 ZIP과 `classroom` Edge Function을 둘 다 다시 배포합니다.

```powershell
python tools/build_static.py --supabase-url "https://프로젝트ID.supabase.co"
npx supabase functions deploy classroom --use-api
```

각 선생님은 같은 사이트의 `/teacher.html`에서 **관리 계정 선택 + 자기 비밀번호**로 로그인합니다. Supabase 대시보드 계정을 네 선생님에게 공유할 필요는 없습니다. 학생에게는 자기 반 관리실에서 발급한 링크만 전달합니다.

## 4. 반별 백업과 복원

관리실의 학급 전체 백업은 로그인한 선생님의 반만 포함합니다. JSON 형식 버전은 2이고 `classId`로 해당 반을 표시합니다. 학생 ID는 내부에서 네 반 전체에 걸쳐 구분하지만, 화면에 보이는 학생 번호는 반마다 1번부터 시작합니다.

복원할 때에는 **백업에 들어 있는 반 번호와 같은 `--class-id`**를 지정합니다.

```powershell
python tools/restore_supabase.py "2반백업.json" --class-id 2
```

비밀번호·학생 기록·로그인 세션·링크 변경은 지정한 반에만 적용됩니다. 다른 반의 기록이나 로그인은 그대로 유지됩니다. 지정한 반의 현재 기록은 먼저 백업하고, 학생 명단·기록 교체는 한 트랜잭션으로 처리합니다. 해당 반의 선생님 비밀번호는 유지하며 로그인 세션을 해제하고 학생 링크를 새로 만듭니다. 다른 반 번호의 백업을 선택하면 DB에 연결하기 전에 중단합니다.

기존 단일 학급 백업(버전 1)은 첫 번째 반의 백업으로 취급합니다. `--class-id 1`로 복원할 수 있습니다. 같은 반 번호의 다른 Supabase 프로젝트로 옮길 때도 먼저 목적지의 학급을 생성해야 합니다.

## 저장 용량

네 반은 Supabase 프로젝트의 DB·전송량·함수 호출 한도를 함께 사용합니다. 학생당 5MB 제한은 유지됩니다. 28명씩 네 반이 모두 상한까지 저장하면 학생 데이터만 약 560MB이므로 무료 DB 500MB를 넘습니다. 실제 사진 크기와 사용량을 대시보드에서 확인하고 정기적으로 백업하세요. 계정 분리가 저장 용량을 네 배로 늘려 주는 것은 아닙니다.

## 검증 범위

자동 테스트는 네 계정의 로그인, 반별 학생 저장·백업, 다른 반 ID를 직접 요청하는 시도, 보고서·QR·명단 접근 제한, 반 이름·비밀번호·링크 변경의 영향 범위를 검사합니다. 기존 데이터가 있는 DB의 업그레이드와 빈 DB의 4반 생성도 검사합니다. 실제 Supabase에 SQL을 적용하고 Edge Function을 배포하는 작업은 별도로 수행해야 합니다.
